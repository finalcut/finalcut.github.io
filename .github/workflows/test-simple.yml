name: Test Workflow

on:
  workflow_dispatch:

jobs:
  test:
    name: Simple Test
    runs-on: ubuntu-latest
    steps:
      - name: Simple test
        run: |
          echo "Test workflow is working!"
          echo "Date: $(date)"
          echo "GitHub Actor: ${{ github.actor }}"